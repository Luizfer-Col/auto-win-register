export type FormData = {
    name: string;
    lastname: string;
    cedula: string;
    phone: string;
    email: string;
    department: string;
    city: string;
    policy: boolean;
  };

  export type QueryData = {
    code: string;
  };
  